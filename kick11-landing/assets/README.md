Drop the logo file here as `logo.png` (or `logo.svg`).

`index.html` already references `assets/logo.png` in the header and footer —
just add the file with that exact name and it will appear automatically.
